# Dockerfile

FROM node:slim

MAINTAINER me@nalbam.com

ENV TZ=Asia/Seoul
RUN ln -snf /usr/share/zoneinfo/$TZ /etc/localtime && echo $TZ > /etc/timezone

EXPOSE 3000

WORKDIR data

COPY app.js .
COPY package.json .
COPY views .

RUN npm install -s

CMD ["node", "app.js"]
